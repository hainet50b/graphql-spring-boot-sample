type Book {
  id: ID!
  title: String!
  publisher: Publisher!
  authors: [Author]!
}

extend type Query {
  findBooks: [Book]!
}

extend type Mutation {
  createBook(title: String!, publisherId: Int!, authorId: Int!): Book!
  relateAuthorToBook(bookId: Int!, authorId: Int!): Boolean!
}
